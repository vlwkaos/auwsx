# auwsx issue 1 merge memo - 2026-06-24

## Shipped

- Cleaned the project config form labels so stored field keys and user-facing copy are decoupled.
- Kept repository suggestions anchored to the repository input field instead of rendering below unrelated form content.
- Added cursor-aware text editing coverage and repository suggestion tests for renamed labels and non-zero repository field positions.

## Verification

- `cargo fmt --package auwsx-tui -- --check`
- `cargo test --workspace`

## Merge Notes

- Rebased `auwsx/issue-1` onto local `main` after `main` advanced with the issue-2 merge.
- Integrated with a no-ff merge commit so the issue history remains grouped on `main`.
