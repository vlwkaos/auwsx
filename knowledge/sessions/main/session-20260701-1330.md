---
slug: remote-sync-tui-visibility
kind: session
title: Remote sync outcomes in TUI project overview
description: Added project-level TUI visibility for recent remote sync runs and failures.
keywords: [remote sync, remote_sync_runs, tui overview, project detail, github, failed sync, active sync, RemoteSyncRun, remote_sync_summary]
created: 2026-07-01
modified: 2026-07-01
---

target_slugs: remote-sync-tui-visibility
plan_feature: remote git repository interface

## remote-sync-tui-visibility

- Added `App::remote_sync_runs` cache populated through `Command::RecentRemoteSyncRuns` during per-project refresh.
- Added pure `ui::vm::remote_sync_summary` projection for active count, failure count, recent target, and error/summary text.
- Added project overview `Remote Sync` panel showing configured-but-idle state, active queued/running work, recent failures, and recent inbound/outbound run rows.
- Kept rendering separate from domain derivation: `overview.rs` renders `SummaryRow` values, while `vm.rs` owns sync-run presentation.
- Tests added:
  - `ui::vm::tests::given_remote_sync_runs_when_projected_then_active_failures_and_messages_are_visible`
  - `ui::overview::tests::given_remote_sync_runs_when_lines_built_then_status_and_failures_are_visible`

## plan

### remote git repository interface

- [x] Per-project remote config popup and CLI paths.
- [x] Inbound `/auwsx-run` backlog creation path.
- [x] Outbound remote issue and PR sync queue/executor paths.
- [x] GitHub webhook adapter.
- [x] PR merge observation marks merged remote PR issues done.
- [x] TUI project overview shows recent remote sync outcomes/failures.
- [ ] Explicit TUI check-status visibility/enforcement remains pending.
- [ ] Full live remote e2e through auwsx pipeline remains pending.

## Trials & Solutions

- Tried checking for `16.1` downgrade source in repo and hidden files -> no match in tree or reachable history -> likely external machine/tool manager state, not this repo.
- Ran `/good-to-go commit` gather after verification -> helper exits nonzero after printing audit inputs -> treated output as audit data and continued because tests/build/diff checks passed.

## Verification

- `cargo fmt --package auwsx-core --package auwsx-tui --package auwsx-web`
- `cargo test --package auwsx-tui remote_sync`
- `cargo test --package auwsx-tui given_remote_sync_runs_when_lines_built_then_status_and_failures_are_visible`
- `cargo test --package auwsx-tui`
- `cargo test --package auwsx-core -- --test-threads=1`
- `cargo test --package auwsx-web`
- `cargo build --workspace`
- `cargo test --package auwsx-core --package auwsx-tui --package auwsx-web --no-run`
- `git diff --check`
- `rg -n "Color::" crates/auwsx-tui/src/ui/ -g !theme.rs`
- `bash /Users/eliot/dotfiles/claude/.claude/skills/good-to-go/scripts/gather.sh commit`
