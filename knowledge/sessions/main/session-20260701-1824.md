---
slug: tui-remove-legacy-backlog-tab
kind: session
title: TUI legacy Backlog tab removed
description: Removed the obsolete standalone Backlog view and cursor so backlog interaction stays in the main project tree and right detail pane.
keywords: [tui, backlog tab, View::Backlog, backlog_sel, operator console, project tree, right pane, ui simplification]
created: 2026-07-01
modified: 2026-07-01
---

target_slugs: [tui-remove-legacy-backlog-tab]
plan_feature: UI overhaul

## tui-remove-legacy-backlog-tab

- Removed `View::Backlog` from the tab cycle and deleted the unused `ui/backlog.rs` renderer.
- Removed `App::backlog_sel`; backlog item selection now lives in the project tree/kanban context.
- Kept backlog list/detail behavior through the Overview tree and right-side backlog panel.
- Reworked `render_revision` away from a hand-counted format string after removing the field.

## pending

- [x] Legacy backlog tab cleanup committed and pushed as `90b5da6 refactor(tui): remove legacy backlog tab`.
- [ ] Continue broader UI overhaul audit; remaining work includes deeper stack-state consistency and schema/data-model simplification beyond this TUI state removal.

## Trials & Solutions

- Tried replacing `render_revision` with one large debug tuple -> Rust tuple `Debug` arity limit failed -> used a vector of formatted fields joined with `|` instead.
- Ran TUI view/action exhaustiveness greps after removing the view; no stale `View::Backlog`, `backlog_sel`, or `ui/backlog.rs` references remained outside valid backlog-domain names.
