---
slug: codex-project-bootstrap
kind: session
title: Bootstrap auwsx project row for Codex
description: Session handoff recording the current TUI v0.1 dirty tree check and creation of the live auwsx project row using Codex agent commands.
keywords: [codex project row, auwsx project add, state.db, main_agent_cmd, plan_agent_cmd, work_agent_cmd, review_agent_cmd, TUI v0.1 handoff, memo project-mode, daemon socket sandbox]
created: 2026-06-10
modified: 2026-06-10
target_slugs: [db-schema, cli-parse-grammar, scheduler-pipeline, agent-subprocess-runner, good-to-go-axes]
plan_feature: tui-v0.1
---

# codex-project-bootstrap

## codex-project-bootstrap

- Checked the dirty tree from the Claude handoff on `main`: TUI v0.1 changes are still uncommitted, including the large `app.rs`/`input.rs` implementation, new `ui/{overview,issue,backlog,logs}.rs`, deleted old widget/editor stubs, and the prior `knowledge/coding/good-to-go-axes.md` update.
- Loaded `/memo` instructions and detected `project-mode` knowledge at `/Users/vlwkaos/ws/auwsx/knowledge`.
- The plan file `~/.claude/plans/current-wsx-is-agent-cosmic-gadget.md` is stale and points to `knowledge/sessions/main/session-2026-06-09-1333.md`; that session file is no longer present, so canonical `knowledge/{coding,domain,history}` plus the current code are the operative context.
- Created the live daemon state DB by starting `cargo run --bin auwsx -- daemon`.
- Added project `1`:
  - name: `auwsx`
  - repo: `/Users/vlwkaos/ws/auwsx`
  - branch: `main`
  - main/plan/work/review command: `codex exec --sandbox workspace-write --json {prompt}`
  - policy remained default/manual: `completion_policy=manual`, `plan_gate_timeout_min=10`, `completion_soft_timeout_min=60`, `max_concurrency=1`
- Verified the row through both `auwsx project get 1` and direct SQLite read of `~/Library/Application Support/auwsx/state.db`.

## pending

- [x] Inspect current dirty tree and confirm the TUI v0.1 handoff state.
- [x] Run memo context detection and record a project-mode session file.
- [x] Initialize live auwsx daemon state and add a Codex-backed project row for this repo.
- [ ] Decide whether the TUI v0.1 slice plus the earlier project-get fix should be committed together or split.
- [ ] Consider adding a supported `project update` IPC/CLI command later; current project command surface can add/list/get but cannot edit existing agent command fields.
- [ ] Add a mock-daemon integration test later for TUI async apply/refresh/event-stream wiring.

## plan

- `tui-v0.1`: current implementation is uncommitted and appears to be the active slice. Next action is a commit decision or a focused follow-up fix, not a broad rewrite.

## Trials & Solutions

- Tried reading the session file named by the stale plan -> file was absent -> used canonical knowledge files and current code as the source of truth.
- Tried starting the daemon in the workspace sandbox -> DB parent creation under `~/Library/Application Support/auwsx` failed with `Operation not permitted` -> reran the daemon with approved escalation because auwsx state intentionally lives outside the repo.
- Tried querying the daemon socket from the sandbox -> Unix socket connect under `~/Library/Caches/auwsx/auwsx.sock` failed with `Operation not permitted` -> used approved escalated `cargo run --bin auwsx -- ...` client commands for the live project row.
