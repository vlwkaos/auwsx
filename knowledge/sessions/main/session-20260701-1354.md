---
slug: remote-pipeline-comment-sync-e2e
kind: session
title: Remote pipeline comment sync e2e assertion
description: Added daemon pipeline e2e coverage proving remote issue comment sync is exercised.
keywords: [remote pipeline, github comments, issue comment sync, daemon e2e, fake gh, local_merge_e2e, remote workflow, project merge]
created: 2026-07-01
modified: 2026-07-01
---

target_slugs: remote-pipeline-comment-sync-e2e
plan_feature: remote git repository interface

## remote-pipeline-comment-sync-e2e

- Extended the deterministic remote daemon e2e to assert the fake GitHub CLI log contains `issue comment`.
- The e2e now proves one auwsx-driven path covers remote issue creation, issue comment sync, PR creation, PR observation, and merge completion through daemon/CLI/scheduler plumbing.
- The configured auwsx design plan path was absent; fallback plan files were unrelated to auwsx, so the current objective and code remained source of truth.

## plan

### remote git repository interface

- [x] Per-project remote config popup and CLI paths.
- [x] Inbound `/auwsx-run` backlog creation path.
- [x] Outbound remote issue and PR sync queue/executor paths.
- [x] GitHub webhook adapter.
- [x] PR merge observation marks merged remote PR issues done.
- [x] TUI project overview shows recent remote sync outcomes/failures.
- [x] TUI shows PR check status.
- [x] Full daemon e2e covers remote issue, comment, PR, and observation flow with a fake GitHub CLI.

## Trials & Solutions

- Tried relying on existing remote e2e coverage -> it covered issue/PR/check flow but not comment sync -> added an explicit `issue comment` log assertion.
- Tried reading configured plan fallback files -> both were unrelated to auwsx -> used current code and active objective as source of truth.

## Verification

- `cargo fmt --package auwsx-core --package auwsx-tui --package auwsx-web`
- `cargo test --package auwsx-tui deterministic_agent_drives_remote_issue_and_pr_pipeline -- --ignored`
- `cargo test --package auwsx-tui`
- `cargo test --package auwsx-core -- --test-threads=1`
- `cargo test --package auwsx-web`
- `cargo build --workspace`
- `cargo test --package auwsx-core --package auwsx-tui --package auwsx-web --no-run`
- `git diff --check`
- `rg -n "Color::" crates/auwsx-tui/src/ui/ -g !theme.rs`
- `bash /Users/eliot/dotfiles/claude/.claude/skills/good-to-go/scripts/gather.sh commit`
