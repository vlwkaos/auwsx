---
slug: tui-form-footer-controls-cleanup
kind: session
title: TUI form controls moved fully to footer
description: Removed the duplicate in-modal form controls row so project/setup forms stay compact and rely on state-aware footer hints.
keywords: [tui, form controls, footer hints, compact forms, project setup, form_extra_rows, draw_form, auwsx-tui]
created: 2026-07-01
modified: 2026-07-01
---

target_slugs: [tui-form-footer-controls-cleanup]
plan_feature: UI overhaul

## tui-form-footer-controls-cleanup

- Removed the duplicate controls line from `draw_form`; form navigation/edit hints now come from the footer model only.
- Deleted the dead `form_controls_label` helper and its direct unit test.
- Reduced `form_extra_rows` by one row because the modal no longer reserves space for controls.
- Updated compact form rendering tests to assert one footer-owned `nav` hint and no old title-case duplicate controls row.

## pending

- [x] Compact form cleanup committed and pushed as `1dec355 fix(tui): keep form controls in footer`.
- [ ] Continue broader UI overhaul goal audit; this only resolves the duplicate form controls slice.

## Trials & Solutions

- Tried asserting specific footer hints like `(j/k) field` at width 90 -> failed because the footer can truncate later hints at that width -> changed the test to assert the stable structural invariant: exactly one footer-owned `nav` hint and no duplicate modal controls row.
- Ran `/good-to-go commit` helper -> it printed project audit axes and exited `1` without a concrete failing check -> kept the result in the final report and relied on focused tests, full TUI tests, `git diff --check`, and UI color scan.
